[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "shoebox"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
  "beautifulsoup4",
  "chrome_version",
  "db-dtypes",
  "ebay_rest>=1.1.4",
  "google-api-python-client",
  "google-auth",
  "google-cloud-bigquery",
  "google-cloud-storage",
  "numpy",
  "openpyxl",
  "pandas",
  "pydantic",
  "slack-bolt",
  "aiohttp",
  "pyyaml",
  "requests",
  "rich",
  "selenium",
  "setuptools<81",
  "streamlit",
  "unidecode",
  "xmltodict"
]

[project.optional-dependencies]
# Chrome-driven pipelines: the 130point price scraper, the Topps release
# calendar sync, and the TCDB browser. undetected-chromedriver pins itself to
# a locally installed Chrome, so it is only useful on a workstation that has
# one -- and it is the single dependency here that builds from a legacy
# setup.py, which fails outright on newer setuptools. Keeping it out of the
# base set means a container image (and CI) installs cleanly without Chrome,
# while a local install adds it explicitly:
#
#   pip install -e ".[scrapers]"
scrapers = [
  "undetected-chromedriver",
]
dev = [
  "pytest>=8",
  "ruff>=0.6",
  "pre-commit>=3.7",
  "types-PyYAML",
]

[project.scripts]
shoebox = "shoebox.cli:main"

[tool.setuptools.packages.find]
where = ["."]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# __init__.py files intentionally re-export names for the public API.
"__init__.py" = ["F401"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
