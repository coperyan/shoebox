    SELECT
      card_id,
      subset_id,
      set_year,
      set_name,
      subset_name,
      subset_type,
      derived_card_number,
      card_number,
      player,
      team,
      note,
      note_check
    FROM `cards.v_checklist`